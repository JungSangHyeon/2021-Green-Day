<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/trackListFragment">

    <fragment
        android:id="@+id/trackListFragment"
        android:name="com.example.greenday.TrackListFragment"
        android:label="@string/main_menu_track_list"
        tools:layout="@layout/fragment_track_list" />
    <fragment
        android:id="@+id/favoriteFragment"
        android:name="com.example.greenday.FavoriteFragment"
        android:label="@string/main_menu_favorite"
        tools:layout="@layout/fragment_favorite" /><action android:id="@+id/action_global_trackListFragment" app:destination="@id/trackListFragment"/><action android:id="@+id/action_global_favoriteFragment" app:destination="@id/favoriteFragment"/>
</navigation>